services:
  - type: web
    name: usta-elbek-bot
    env: python
    region: frankfurt
    plan: free
    
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    
    startCommand: python main.py
    
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: RENDER
        value: true
    
    # ✅ PORT ni aniq belgilash
    envVars:
      - key: PORT
        value: 8080
    
    healthCheckPath: /health
    autoDeploy: true